# Project index (pointer)

The **full project registry** (263 catalog rows: purpose, summary counts, every domain table, deduplication notes, and contribution reminders) lives in **[`README.md` — Project catalog](README.md#project-catalog)**.

**Contributors:** add or edit rows in **`README.md`** inside the `## Project catalog` section so the GitHub landing page stays complete. Keep summary counts in sync with the tables.

**Why this file exists:** Stable path and deep links for tools or docs that still reference `PROJECT_INDEX.md`.
