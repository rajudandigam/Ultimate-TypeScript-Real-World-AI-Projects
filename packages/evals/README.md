# @repo/evals

Reusable **evaluation schemas** and **test harness utilities** for AI workflows and agents.

**Status:** scaffold only — align with `benchmarks/` and golden-set practices as they land.

## Scripts

- `pnpm typecheck`
- `pnpm test`
