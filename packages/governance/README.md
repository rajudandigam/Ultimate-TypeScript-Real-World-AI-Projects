# @repo/governance

Reusable **governance primitives** for cost tracking, audit logging, PII redaction, guardrails, rate limits, and policy decisions.

**Status:** scaffold only — integrate with observability and policy engines as reference implementations mature.

## Scripts

- `pnpm typecheck`
- `pnpm test`
