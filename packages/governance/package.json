{
  "name": "@repo/governance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Governance primitives: cost tracking, audit logging, PII redaction, guardrails, rate limits, policy decisions (stubs).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.7.3",
    "vitest": "^3.0.5"
  }
}
