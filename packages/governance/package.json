{
  "name": "@repo/governance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference governance primitives: cost estimates, audit trails, heuristic PII redaction, guardrails, rate limits, policy records (not compliance software).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "~5.7.3",
    "vitest": "^3.0.5"
  }
}
