# Benchmarking

Methodology for comparing workflows and agents: datasets, harness design, regression gates, and how benchmarks relate to `benchmarks/` and `@repo/evals`.

## Runnable harness

The workspace package **[`benchmarks/`](../benchmarks/README.md)** (`@repo/benchmarks`) contains the reusable runner, statistics helpers, result writers, and per-flagship suites (for example cost monitoring). Read **[`benchmarks/methodology.md`](../benchmarks/methodology.md)** before interpreting or publishing any numbers.
