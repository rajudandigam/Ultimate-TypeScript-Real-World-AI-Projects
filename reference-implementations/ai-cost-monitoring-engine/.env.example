# Copy to .env for local overrides (optional; the demo runs without this file).

# Reserved for future wiring: path to a JSON file exporting PricingConfig for @repo/governance.
# PRICING_CONFIG_PATH=

# Optional log verbosity for the CLI (info | debug).
# LOG_LEVEL=info
