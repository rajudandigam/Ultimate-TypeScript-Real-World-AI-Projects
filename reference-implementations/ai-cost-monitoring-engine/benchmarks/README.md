# Reference-local micro-benchmark

`cost-benchmark.ts` is a **tiny** in-package timing script for quick manual checks while editing the flagship.

For the **shared harness** (runner, statistics, JSON/Markdown writers, suite orchestration), use the repository workspace:

[`../../../benchmarks/README.md`](../../../benchmarks/README.md)
